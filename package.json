{
  "name": "url-redirector",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build:watch": "tsc --watch",
    "build": "ts-node --project tsconfig.scripts.json scripts/build.ts",
    "bundle": "ts-node --project tsconfig.scripts.json scripts/bundle.ts",
    "headful": "ts-node --project tsconfig.scripts.json demos/headful-session.ts",
    "record:demo": "./demos/record-demo.sh",
    "test:install": "playwright install --with-deps chromium",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:coverage": "npm run test && npx monocart show-report test/coverage/index.html",
    "update-manifest": "jq --arg v $npm_package_version '.version = $v' manifest.json > tmp.json && mv tmp.json manifest.json",
    "version": "npm run update-manifest && git add manifest.json",
    "postversion": "git push && git push --tags && gh release create v$npm_package_version --generate-notes",
    "setup-store-auth": "ts-node --project tsconfig.scripts.json scripts/setup-cws-auth.ts",
    "check-store-status": "ts-node --project tsconfig.scripts.json scripts/check-cws-status.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/secret-manager": "^6.1.1",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/adm-zip": "^0.5.7",
    "@types/chrome": "^0.1.31",
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^24.10.1",
    "adm-zip": "^0.5.16",
    "fs-extra": "^11.3.2",
    "monocart-reporter": "^2.9.23",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}